# Ledger data — append-only JSONL files, up to ~300 MB/year.
# Not committed to git to avoid bloating history.
# Store locally or sync via separate backup mechanism.
data_v4/ledger/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# macOS
.DS_Store

# Virtual environments
venv/
.venv/
env/
